Write a program to find the value of nCr given n,r.

Input Format Only line contains two integers n and r.

Output Format Print a single integer denoting the vlue of nCr.

Example 1: 3 2 Output: 3

Constraints 1 <= n <= 20 1 <= r <= n

import java.util.*;
public class Main
{
    public static long factorial(int n){
        long fact = 1;
        for(int i = 1; i<=n; i++){
            fact*=i;
        }
        return fact;
    }
	public static void main(String[] args) {
	    Scanner sc = new Scanner(System.in);
	    int n = sc.nextInt();
	    int r = sc.nextInt();
		System.out.println((factorial(n))/(factorial(n-r)*factorial(r)));
	}
}
